<%- include('../partials/flashmessage') %>

<% if(user){ %>
    <div>Giriş Yapab Kullanıcı <h1><%=user.username  %></h1></div>

<%  }%>







<div class="card"> 
    <h5 class="card-header">Kullanıcılar</h5>
    <div class="card-body">
        <% if (users) { %>
            
    <% } %>
    <% if (users) { %>
        <ul class="list-group">
            <% users.forEach(function(users) { %>
                 <li class="list-group-item"><%=users.username  %></li>
            <% }); %>
        </ul>
    <% } else { %>
       <div class="alert alert-danger"> Kullanıcı yok</div>
    <% } %>
    </div>

</div>